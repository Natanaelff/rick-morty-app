module.exports = {
  project: {
    ios: {},
    android: {},
  },
  // Fonts are bundled automatically: on iOS by the react-native-vector-icons
  // CocoaPod (s.resources), on Android by the .ttf in app/src/main/assets/fonts.
  // No `assets` linking here — that would double-bundle and break the build.
};
